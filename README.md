# cc-pub-fallback

Fallback host for cc-publish.sh. Markdown artifacts published from CC
sessions when the primary Vercel host is unreachable. URLs reach this
content via is.gd / tinyurl shorteners; the bare raw.githubusercontent.com
URL is blocked by Claude chat's web_fetch.
